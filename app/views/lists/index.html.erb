<h1>Welcome page: Index of lists</h1>

<button><%= link_to "Add new list", new_list_path %></button>



<% @lists.each do |l| %>

  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= cl_image_path(l.photo.key, height: 300, width: 400, crop: :fill) if l.photo.attached? %>)">
    <%= link_to l.name, list_path(l) %>
    <button><%= link_to "📌 Add bookmark", new_list_bookmark_path(l) %></button>
  </div>
<% end %>
